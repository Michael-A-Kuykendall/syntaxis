From /home/michael/repos/syntaxis: `cargo test --all-targets`; `cargo fmt --all -- --check`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo run --example demo -p engine-cli` after CLI wiring. `cargo metadata --no-deps` is useful for workspace inspection. The demo currently depends on running from workspace root because it reads fixtures/challenge_agreement.conllu.