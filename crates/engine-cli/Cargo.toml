[package]
name = "engine-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Offline command line front end for the Syntaxis workspace."

[[bin]]
name = "syntaxis"
path = "src/main.rs"

[dependencies]
parser-core = { version = "0.1.0", path = "../parser-core" }
english-rules = { version = "0.1.0", path = "../english-rules" }
conllu = { version = "0.1.0", path = "../conllu" }
