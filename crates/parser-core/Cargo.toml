[package]
name = "parser-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Stable data model, spans, supports, and the retraction-capable fact graph."

[dependencies]
