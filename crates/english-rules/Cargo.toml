[package]
name = "english-rules"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic English normalization, segmentation, tokenization, and rule packs."

[dependencies]
parser-core = { path = "../parser-core" }
